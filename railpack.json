{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "...",
        { "customName": "Create Playwright browsers dir", "cmd": "mkdir -p /app/pw-browsers" },
        { "customName": "Install Playwright Chromium", "cmd": "python -m playwright install --with-deps chromium" }
      ]
    }
  }
}
