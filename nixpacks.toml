[phases.setup]
# Sagt Railway, dass wir den PDF-Drucker brauchen
nixPkgs = ["wkhtmltopdf"]

[phases.build]
# Installiert die Python-Pakete UND den Playwright-Browser mit allen System-Abhängigkeiten
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium --with-deps"
]
